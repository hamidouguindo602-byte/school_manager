import ResourcePage from '../../components/common/ResourcePage';
import { createAnneeScolaire, deleteAnneeScolaire, getAnneesScolaires, updateAnneeScolaire } from '../../api/anneesScolaires.api';
export default function AnneesScolairesPage() { return <ResourcePage title="Années scolaires" description="Gérez les périodes scolaires." loader={getAnneesScolaires} createAction={createAnneeScolaire} updateAction={updateAnneeScolaire} deleteAction={deleteAnneeScolaire} formFields={[{ name: 'libelle', label: 'Libellé', required: true }, { name: 'dateDebut', label: 'Début', type: 'date', required: true }, { name: 'dateFin', label: 'Fin', type: 'date', required: true }, { name: 'statut', label: 'Statut', required: true }]} columns={[{ label: 'Libellé', key: 'libelle' }, { label: 'Début', key: 'dateDebut' }, { label: 'Fin', key: 'dateFin' }, { label: 'Statut', key: 'statut' }]} />; }
