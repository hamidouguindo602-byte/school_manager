import ResourcePage from '../../components/common/ResourcePage';
import { createParent, deleteParent, getParents, updateParent } from '../../api/parents.api';
export default function ParentsPage() { return <ResourcePage title="Parents" description="Gérez les responsables légaux." loader={getParents} createAction={createParent} updateAction={updateParent} deleteAction={deleteParent} formFields={[{ name: 'prenom', label: 'Prénom', required: true }, { name: 'nom', label: 'Nom', required: true }, { name: 'numeroTelephone', label: 'Téléphone', required: true }, { name: 'email', label: 'Email', type: 'email' }, { name: 'motDePasse', label: 'Mot de passe', type: 'password', required: true }]} columns={[{ label: 'Nom', render: (item) => `${item.prenom || ''} ${item.nom || ''}` }, { label: 'Email', key: 'email' }, { label: 'Téléphone', key: 'numeroTelephone' }]} />; }
