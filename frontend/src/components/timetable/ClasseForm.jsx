export default function ClasseForm({ value = {}, onChange, onSubmit }) { return <form className="card-panel entity-form" onSubmit={onSubmit}><div className="form-grid"><label className="field">Nom de classe<input required value={value.nomClasse || ''} onChange={(event) => onChange({ ...value, nomClasse: event.target.value })} /></label><label className="field">Niveau<input required value={value.niveau || ''} onChange={(event) => onChange({ ...value, niveau: event.target.value })} /></label></div><button className="primary-button">Enregistrer</button></form>; }
