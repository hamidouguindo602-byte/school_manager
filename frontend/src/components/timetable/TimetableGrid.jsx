export default function TimetableGrid({ grid = {} }) { return <div className="schedule-grid">{['LUNDI','MARDI','MERCREDI','JEUDI','VENDREDI'].map((day) => <section className="schedule-day" key={day}><h2>{day}</h2>{(grid[day] || []).map((slot) => <article className="schedule-slot" key={slot.id}><strong>{slot.nomMatiere}</strong><span>{slot.heureDebut} - {slot.heureFin}</span><small>{slot.nomEnseignant} {slot.prenomEnseignant}</small></article>)}</section>)}</div>; }
