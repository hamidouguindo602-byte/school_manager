export default function AssignationForm({ value = {}, onChange, onSubmit }) { return <form className="card-panel entity-form" onSubmit={onSubmit}><div className="form-grid">{[['classeId','ID classe'],['matiereId','ID matière'],['enseignantId','ID enseignant'],['heuresHebdomadaires','Heures/semaine']].map(([name, label]) => <label className="field" key={name}>{label}<input required type="number" value={value[name] || ''} onChange={(event) => onChange({ ...value, [name]: event.target.value })} /></label>)}</div><button className="primary-button">Enregistrer</button></form>; }
