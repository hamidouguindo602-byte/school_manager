export default function MatiereForm({ value = {}, onChange, onSubmit }) { return <form className="card-panel entity-form" onSubmit={onSubmit}><div className="form-grid"><label className="field">Nom<input required value={value.nomMatiere || ''} onChange={(event) => onChange({ ...value, nomMatiere: event.target.value })} /></label><label className="field">Code<input required value={value.code || ''} onChange={(event) => onChange({ ...value, code: event.target.value })} /></label><label className="field">Coefficient<input required type="number" value={value.coefficient || ''} onChange={(event) => onChange({ ...value, coefficient: event.target.value })} /></label></div><button className="primary-button">Enregistrer</button></form>; }
