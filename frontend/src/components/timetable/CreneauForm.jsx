export default function CreneauForm({ value = {}, onChange, onSubmit }) { return <form className="card-panel entity-form" onSubmit={onSubmit}><div className="form-grid"><label className="field">ID assignation<input required type="number" value={value.assignationId || ''} onChange={(event) => onChange({ ...value, assignationId: event.target.value })} /></label><label className="field">Jour<input required value={value.jour || ''} onChange={(event) => onChange({ ...value, jour: event.target.value })} /></label><label className="field">Début<input required type="time" value={value.heureDebut || ''} onChange={(event) => onChange({ ...value, heureDebut: event.target.value })} /></label><label className="field">Fin<input required type="time" value={value.heureFin || ''} onChange={(event) => onChange({ ...value, heureFin: event.target.value })} /></label></div><button className="primary-button">Enregistrer</button></form>; }
