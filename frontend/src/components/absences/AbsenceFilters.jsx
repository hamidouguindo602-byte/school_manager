export default function AbsenceFilters({ students = [], value, onChange }) { return <div className="card-panel absence-filters"><label className="field">Élève<select value={value} onChange={(event) => onChange(event.target.value)}><option value="">Tous les élèves</option>{students.map((item) => <option key={item.id} value={item.id}>{item.prenom} {item.nom}</option>)}</select></label></div>; }
