export default function AnnonceForm({ value = {}, onChange, onSubmit }) { return <form className="card-panel entity-form" onSubmit={onSubmit}><label className="field">Titre<input required value={value.titre || ''} onChange={(event) => onChange({ ...value, titre: event.target.value })} /></label><label className="field">Contenu<textarea rows="4" value={value.contenu || ''} onChange={(event) => onChange({ ...value, contenu: event.target.value })} /></label><label className="field">PDF<input type="file" accept="application/pdf" onChange={(event) => onChange({ ...value, fichier: event.target.files[0] })} /></label><button className="primary-button">Publier</button></form>; }
