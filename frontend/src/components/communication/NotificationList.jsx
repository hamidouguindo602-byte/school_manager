export default function NotificationList({ items = [], onRead, onDelete }) { return <div className="shortcut-list">{items.map((item) => <article className="shortcut-item" key={item.id}><span><strong>{item.titre}</strong><small>{item.message}</small></span>{!item.lu && <button className="table-action" onClick={() => onRead(item.id)}>Lu</button>}<button className="table-action danger-action" onClick={() => onDelete(item.id)}>Supprimer</button></article>)}</div>; }
