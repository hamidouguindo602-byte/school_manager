export default function ConfirmDialog({ open, title = 'Confirmer', message, onConfirm, onCancel }) { if (!open) return null; return <div className="dialog-backdrop"><div className="dialog-card"><h2>{title}</h2><p>{message}</p><div className="dialog-actions"><button className="secondary-button" onClick={onCancel}>Annuler</button><button className="primary-button" onClick={onConfirm}>Confirmer</button></div></div></div>; }
