export default function DataTable({ columns = [], rows = [], empty = 'Aucune donnée.' }) { return <div className="table-wrap"><table><thead><tr>{columns.map((column) => <th key={column.label}>{column.label}</th>)}</tr></thead><tbody>{rows.map((row, index) => <tr key={row.id || index}>{columns.map((column) => <td key={column.label}>{column.render ? column.render(row) : row[column.key] ?? '—'}</td>)}</tr>)}{!rows.length && <tr><td className="state-message" colSpan={columns.length}>{empty}</td></tr>}</tbody></table></div>; }
