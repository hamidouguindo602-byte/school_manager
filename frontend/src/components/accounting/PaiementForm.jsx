export default function PaiementForm({ value = {}, onChange, onSubmit }) { return <form className="card-panel entity-form" onSubmit={onSubmit}><div className="form-grid"><label className="field">ID échéance<input required type="number" value={value.idEcheance || ''} onChange={(event) => onChange({ ...value, idEcheance: event.target.value })} /></label><label className="field">Montant<input required type="number" value={value.montant || ''} onChange={(event) => onChange({ ...value, montant: event.target.value })} /></label><label className="field">Mode de paiement<input required value={value.modePaiement || ''} onChange={(event) => onChange({ ...value, modePaiement: event.target.value })} /></label></div><button className="primary-button">Enregistrer</button></form>; }
