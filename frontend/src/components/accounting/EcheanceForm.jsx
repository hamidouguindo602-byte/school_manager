export default function EcheanceForm({ value = {}, onChange, onSubmit }) { return <form className="card-panel entity-form" onSubmit={onSubmit}><div className="form-grid"><label className="field">ID inscription<input required type="number" value={value.idInscription || ''} onChange={(event) => onChange({ ...value, idInscription: event.target.value })} /></label><label className="field">Libellé<input required value={value.libelle || ''} onChange={(event) => onChange({ ...value, libelle: event.target.value })} /></label><label className="field">Montant<input required type="number" value={value.montant || ''} onChange={(event) => onChange({ ...value, montant: event.target.value })} /></label><label className="field">Date limite<input required type="date" value={value.dateLimite || ''} onChange={(event) => onChange({ ...value, dateLimite: event.target.value })} /></label></div><button className="primary-button">Enregistrer</button></form>; }
