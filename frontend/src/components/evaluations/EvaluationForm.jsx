export default function EvaluationForm({ value = {}, onChange, onSubmit }) { return <form className="card-panel entity-form" onSubmit={onSubmit}><div className="form-grid">{[['idMatiere','ID matière'],['idEnseignant','ID enseignant'],['idClasse','ID classe']].map(([name, label]) => <label className="field" key={name}>{label}<input required type="number" value={value[name] || ''} onChange={(event) => onChange({ ...value, [name]: event.target.value })} /></label>)}<label className="field">Date<input required type="date" value={value.dateEvaluation || ''} onChange={(event) => onChange({ ...value, dateEvaluation: event.target.value })} /></label><label className="field">Type<input required value={value.type || ''} onChange={(event) => onChange({ ...value, type: event.target.value })} /></label></div><button className="primary-button">Enregistrer</button></form>; }
