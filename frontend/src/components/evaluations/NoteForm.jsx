export default function NoteForm({ value = {}, onChange, onSubmit }) { return <form className="card-panel entity-form" onSubmit={onSubmit}><div className="form-grid"><label className="field">ID évaluation<input required type="number" value={value.idEvaluation || ''} onChange={(event) => onChange({ ...value, idEvaluation: event.target.value })} /></label><label className="field">ID élève<input required type="number" value={value.idEleve || ''} onChange={(event) => onChange({ ...value, idEleve: event.target.value })} /></label><label className="field">Note / 20<input required min="0" max="20" step="0.01" type="number" value={value.valeur || ''} onChange={(event) => onChange({ ...value, valeur: event.target.value })} /></label></div><button className="primary-button">Enregistrer</button></form>; }
