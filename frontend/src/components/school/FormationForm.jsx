export default function FormationForm({ value = {}, onChange, onSubmit }) { return <form className="card-panel entity-form" onSubmit={onSubmit}><div className="form-grid"><label className="field">Nom<input required value={value.nomFormation || ''} onChange={(event) => onChange({ ...value, nomFormation: event.target.value })} /></label><label className="field">Durée<input required type="number" value={value.duree || ''} onChange={(event) => onChange({ ...value, duree: event.target.value })} /></label></div><label className="field">Description<textarea value={value.description || ''} onChange={(event) => onChange({ ...value, description: event.target.value })} /></label><button className="primary-button">Enregistrer</button></form>; }
