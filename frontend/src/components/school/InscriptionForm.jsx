export default function InscriptionForm({ value = {}, onChange, onSubmit }) { return <form className="card-panel entity-form" onSubmit={onSubmit}><div className="form-grid">{[['idEleve','ID élève'],['idFormation','ID formation'],['idClasse','ID classe']].map(([name, label]) => <label className="field" key={name}>{label}<input required type="number" value={value[name] || ''} onChange={(event) => onChange({ ...value, [name]: event.target.value })} /></label>)}<label className="field">Statut<select value={value.statut || 'EN_ATTENTE'} onChange={(event) => onChange({ ...value, statut: event.target.value })}><option>EN_ATTENTE</option><option>VALIDE</option><option>ANNULE</option></select></label></div><button className="primary-button">Enregistrer</button></form>; }
