export default function EleveForm({ value = {}, onChange, onSubmit }) { return <form className="card-panel entity-form" onSubmit={onSubmit}><div className="form-grid">{[['prenom','Prénom'],['nom','Nom'],['numeroTelephone','Téléphone'],['email','Email']].map(([name, label]) => <label className="field" key={name}>{label}<input required={name !== 'email'} type={name === 'email' ? 'email' : 'text'} value={value[name] || ''} onChange={(event) => onChange({ ...value, [name]: event.target.value })} /></label>)}<label className="field">Date de naissance<input required type="date" value={value.dateNaissance || ''} onChange={(event) => onChange({ ...value, dateNaissance: event.target.value })} /></label></div><button className="primary-button">Enregistrer</button></form>; }
